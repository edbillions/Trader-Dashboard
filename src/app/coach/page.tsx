import { PageHeader } from "@/components/layout/page-header";

export default function CoachPage() {
  return (
    <div>
      <PageHeader
        title="Coach"
        description="Streaks, discipline score, tilt signals, coming in Phase 5."
      />
    </div>
  );
}
